# 数据库设计文档

## 📋 设计原则

1. **无外键约束** - 所有表关联使用逻辑 ID，避免删除时的级联问题
2. **JSON 灵活存储** - 凭证文件列表、操作详情使用 JSON 字段
3. **H2 嵌入式数据库** - 应用启动时自动初始化

---

## 🗄️ 数据表概览

| 序号 | 表名 | 说明 | 核心字段 |
|------|------|------|----------|
| 1 | `user_account` | 用户账号表 | username, password_hash |
| 2 | `employee` | 人员库 | name, type(正式/兼职) |
| 3 | `equipment` | 设备库 | name, depreciation_rate |
| 4 | `clock_in_record` | 打卡记录 | employee_id, duration_hours |
| 5 | `project` | 项目表 | name, start_time, status |
| 6 | `project_employee` | 项目 - 员工关联 | project_id, employee_id, **coefficient** |
| 7 | `project_equipment` | 项目 - 设备关联 | project_id, equipment_id |
| 8 | `project_settlement` | 项目结算表 | 八类费用字段 |
| 9 | `expense_voucher` | 费用凭证表 | settlement_id, expense_type, **voucher_files(JSON)** |
| 10 | `operation_log` | 操作日志表 | operator_id, **operation_detail(JSON)** |

---

## 🔑 核心设计说明

### 1. 项目 - 员工关联（系数固化）

```sql
project_employee:
  - project_id: 项目 ID
  - employee_id: 员工 ID
  - coefficient: DECIMAL(5,4) 系数（如 0.7200 = 72%）
```

**设计要点**：
- 系数在创建项目关联时**随机生成并固化**
- 后续计算研发工时 = 打卡总时长 × coefficient
- 系数一旦生成不可修改，确保审计追溯

### 2. JSON 字段使用

#### expense_voucher.voucher_files
```json
[
  {
    "name": "工资单.pdf",
    "path": "/uploads/vouchers/2026/02/xxx.pdf",
    "uploadTime": "2026-02-08T14:30:00"
  }
]
```

#### operation_log.operation_detail
```json
{
  "projectName": "AI 算法研发",
  "employees": ["张三", "李四"],
  "devices": ["服务器 01"],
  "changes": {
    "status": {"from": "进行中", "to": "已结算"}
  }
}
```

### 3. 八类费用字段（project_settlement）

| 字段名 | 说明 |
|--------|------|
| labor_cost | 人工费用 |
| direct_input_cost | 直接投入费用 |
| depreciation_cost | 折旧费用 |
| intangible_amortization | 无形资产摊销 |
| design_test_cost | 设计试验费用 |
| outsourcing_cost | 外包合作费用 |
| ip_cost | 知识产权费用 |
| other_cost | 其他相关费用 |
| total_amount | 合计金额 |

---

## 📂 文件清单

```
src/main/
├── java/com/vayen/rdcm/
│   ├── entity/
│   │   ├── UserAccount.java
│   │   ├── Employee.java
│   │   ├── Equipment.java
│   │   ├── ClockInRecord.java
│   │   ├── Project.java
│   │   ├── ProjectEmployee.java
│   │   ├── ProjectEquipment.java
│   │   ├── ProjectSettlement.java
│   │   ├── ExpenseVoucher.java
│   │   └── OperationLog.java
│   └── repository/
│       ├── EmployeeRepository.java
│       ├── EquipmentRepository.java
│       ├── ProjectRepository.java
│       ├── ProjectEmployeeRepository.java
│       ├── ProjectEquipmentRepository.java
│       ├── ProjectSettlementRepository.java
│       ├── ExpenseVoucherRepository.java
│       └── OperationLogRepository.java
└── resources/
    ├── data.sql (建表 + 初始数据)
    └── application.properties
```

---

## 🚀 初始化流程

1. **启动应用** → H2 TCP 服务启动（端口 9092）
2. **自动执行 data.sql** → 创建 10 张表 + 插入测试数据
3. **访问 H2 Console** → http://localhost:8080/h2-console
   - JDBC URL: `jdbc:h2:tcp://localhost:9092/./data/rdcm_db`
   - 用户名：sa
   - 密码：空

---

## 📊 测试数据

| 表 | 数据量 | 说明 |
|----|--------|------|
| user_account | 1 | admin 账号 |
| employee | 3 | 张三（正式）、李四（正式）、王五（兼职） |
| equipment | 3 | 服务器 01、测试电脑 01、开发笔记本 01 |
| clock_in_record | 6 | 近 30 天打卡记录 |
| project | 2 | AI 算法研发、数据分析平台 |
| project_employee | 4 | 项目 - 员工关联（含系数） |
| project_equipment | 3 | 项目 - 设备关联 |

---

## ⚠️ 注意事项

1. **H2 数据库文件位置**: `./data/rdcm_db`（相对于应用启动目录）
2. **TCP 端口**: 9092（在 RdcmApplication.java 中配置）
3. **数据持久化**: H2 文件会持久化，删除需手动清理 `./data/` 目录
4. **生产环境**: 建议切换到 MySQL/PostgreSQL

---

## 📝 后续开发建议

1. ✅ 数据库设计完成
2. ⏭️ 下一步：创建 Service 层业务逻辑
3. ⏭️ 再下一步：创建 Controller 层 REST API
4. ⏭️ 最后：前端 Vue 对接

---

**文档版本**: V1.0  
**更新日期**: 2026-03-07  
**设计者**: AI Assistant
