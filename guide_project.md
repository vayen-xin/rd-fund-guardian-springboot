# 项目开发上下文与技术规范手册 (Project Context & Technical Brief)

## 1. 项目概况 (Project Overview)
本系统是一个企业级管理后台，核心目标是实现**项目月度财务统计**、**员工绩效关联（系数配置）**以及**设备资源管理**。系统已从早期的原型/设计稿模式全面转向纯净的功能驱动模式，强调操作的严谨性与数据的准确性。

## 2. 技术栈 (Tech Stack)
*   **前端框架**: React (Latest) + TypeScript (严格类型校验)
*   **样式方案**: Tailwind CSS v4 (采用最新的 CSS 变量及指令集)
*   **路由管理**: React Router (Data Mode)
*   **图标库**: Lucide-React
*   **图表库**: Recharts
*   **交互组件**: 自定义模态框、二次确认组件、Toast 通知（Sonner）

## 3. 核心开发规范与习惯 (Coding Standards)
*   **组件化思维**: 逻辑复杂的页面需拆分为多个小型函数式组件，存放于 `/src/app/components` 目录下。
*   **命名约定**:
    *   **文件/组件**: 采用 `PascalCase`（如 `ProjectMonthlyStats.tsx`）。
    *   **函数/变量**: 采用 `camelCase`（如 `handleConfirmDelete`）。
    *   **样式**: 优先使用原生的 Tailwind v4 类名，避免编写冗余的 CSS。
*   **类型安全**: 严禁使用 `any`。所有 API 响应、组件 Props 及状态必须定义对应的 `Interface` 或 `Type`。
*   **UI 纯净度**: 系统中已移除所有“模拟数据”、“切换视图”或“查看原型”的按钮。界面必须保持生产环境的高可用状态。

## 4. 核心业务逻辑约束 (Critical Business Rules)
在进行后端接口对接或逻辑扩展时，必须遵循以下硬性规定：
*   **二次确认交互**: **所有**涉及增、删、改的操作（如删除设备、结算项目、更新配置），必须触发 UI 层的二次确认弹窗。
*   **费用录入模式**:
    *   **固定行模式**: 费用录入不再支持前端动态增减行。
    *   **8 大类别**: 必须严格对应系统预设的 8 个固定支出类别（如：人工、材料、租赁等），后端返回及前端提交均以此为准。
*   **权限与安全**:
    *   **分公司管理**: 具备特殊权限，可以修改状态为“已结算”的项目（普通用户不可编辑）。
    *   **账号自保护**: 在账号管理模块中，逻辑上必须拦截并禁止“停用/删除当前登录账号”的操作。
*   **响应式设计**: 默认支持桌面端操作，同时兼顾大屏展示的适配。

## 5. 当前开发进度 (Current Status)
*   **前端重构**: 已完成全部 10 个子页面的重构（包括：月度统计、关联配置、设备管理、账号设置等）。
*   **清理工作**: 彻底移除了“打卡记录导入”、“操作日志”等页面的冗余视图切换逻辑和设计稿展示组件。
*   **状态控制**: 目前所有页面均已适配纯功能视图模式，数据交互逻辑已按照二次确认规范封装。
